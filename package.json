{
  "repository": {
    "type": "git",
    "uri": "https://github.com/Mclilzee/toplsp"
  },
  "name": "toplsp",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/extension",
  "type": "module",
  "scripts": {
    "test": "NODE_OPTIONS=\"$NODE_OPTIONS --experimental-vm-modules\" jest",
    "vscode:prepublish": "npm run esbuild-base -- --minify",
    "esbuild-base": "esbuild ./src/main.js --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
    "esbuild": "npm run esbuild-base -- --sourcemap",
    "esbuild-watch": "npm run esbuild-base -- --sourcemap --watch",
    "test-compile": "tsc -p ./"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.20.2",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "jsonc-parser": "^3.2.1",
    "markdownlint": "^0.34.0"
  },
  "engines": {
    "vscode": "^1.8.0"
  },
  "activationEvents": {}
}
